---
title: "Predicting Sepal Length"
author: "Dennis Wandschura"
date: "2025-07-13"
output: ioslides_presentation
---

```{r setup, include=FALSE}
library(ggplot2)
library(ggiraphExtra)
knitr::opts_chunk$set(echo = FALSE)
 mydata <- iris
```

## App Features

With the app you can predict sepal length with two different models

```{r, echo=TRUE}
    model1 <- lm(Sepal.Length ~ Petal.Length, mydata)
    model2 <- lm(Sepal.Length ~ Petal.Length + Petal.Width, mydata)
```

## How to use

Run the app and use the sliders!
https://denniswandschura.shinyapps.io/PredictingSepaLength/

Source-code
https://github.com/DennisWandschura/DataProductsProject


## Used data

The iris dataset is being used as data
```{r}
     head(mydata)
```

## Plot of used data

- Uses petal length and width as predictor

```{r}
    ggplot(mydata, aes(y = Sepal.Length, x = Petal.Length, color = Species)) + 
            geom_point()
```